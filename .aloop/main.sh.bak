#!/bin/bash
# .aloop/main.sh — 默认定时检测脚本
# 非空 stdout 将作为 prompt 发送给 agent
# A-Loop 脚本测试用例
# 1.正常显示气泡，可以切换tab
# echo "我正在测试 A-Loop 脚本，你收到这条消息后，不用思考，不用回答"
# 2.相同指令连续发送时只显示第一次（用户正常发消息也是一样）
# echo "!ls"
# 3.测试成功
# echo "/goal 显示当前分支名称"

# 4.测试有main.sh文件无输出，A-Loop静默（正常）
# 5.测试无main.sh文件，A-Loop静默（正常）

